export type FeedbackProps = {
    error:boolean,
    message:string
}

export type UserProps={
    name:string,
    email:string,
    username?:string,
    password:string,
    lcd:string,
    role:string,
    title:string,
    department:string,
    photo:string,
    _id:string,
    createdAt:Date,
    gender:string
}