export type FeedbackProps = {
    message:string,
    error:boolean
}

export type UserProps = {
    name:string,
    email:string,
    password:string,
    department:string,
    role:string,
    title:string,
    gender:string,
    photo:string,
    lcd:Date,
    _id:string
}